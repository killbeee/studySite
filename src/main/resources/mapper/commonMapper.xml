<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myProject.myPj.common.mapper.CommonMapper">	<!-- mapper 인터페이스가 위치하는 경로 -->
    <select id="getNextPK" parameterType="hashMap" resultType="String">
     SELECT
			FN_GET_PK( #{alias},#{tableName})
		FROM
			DUAL
    </select>
</mapper>